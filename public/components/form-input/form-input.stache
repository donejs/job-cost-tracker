<can-import from="./form-input.less!" />

<div class="form-group">
	<label for="{{uniqueId}}"><content /></label>
	<input
    id="{{uniqueId}}"
    type="{{type}}"
    class="form-control"
    {($value)}="value"
    {{#if placeholder}}placeholder="{{placeholder}}"{{/if}}
  />
</div>
