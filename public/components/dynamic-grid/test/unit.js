import 'steal-mocha';
import chai from 'chai';

const assert = chai.assert;

import VM from '../dynamic-grid';

describe('<dynamic-grid>', () => {
  describe('ViewModel', () => {
    it('', () => {
      console.dir(assert);
    });
  });
});
